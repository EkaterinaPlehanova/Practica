#include <iostream>
#include <cmath>
int main()
{
    long double a;
    long double b;
    std::cout << "";
    std::cin >> a;
    std::cout << "";
    std::cin >> b;
    std::cout.precision(20);
    std::cout <<sqrt(pow(a,2)+ pow(b, 2));
    return 0;
}
